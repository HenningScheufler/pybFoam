Tutorials
=========

End-to-end walkthroughs that build a skill step by step. Each tutorial
runs at documentation build time against the cases shipped under
``examples/``, so the output you see on the page is exactly what the
script produced.

Start at the top and work down — later tutorials build on the first.
