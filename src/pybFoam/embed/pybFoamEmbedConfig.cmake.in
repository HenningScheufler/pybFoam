@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Re-find dependencies that pybFoamEmbed was built against
find_dependency(Python COMPONENTS Interpreter Development.Module Development.Embed)
find_dependency(OpenFOAM)
find_dependency(nanobind CONFIG)

# Import the exported targets
include("${CMAKE_CURRENT_LIST_DIR}/pybFoamEmbedTargets.cmake")
