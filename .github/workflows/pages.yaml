name: Deploy Docs to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up OpenFOAM
        uses: gerlero/setup-openfoam@v1
        with:
          openfoam-version: 2412
      - name: Install uv
        uses: astral-sh/setup-uv@v6
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # PyVista's official action: installs xvfb on Linux, sets the
      # offscreen environment vars, works cross-platform. Without this,
      # the gallery scripts that call pv.Plotter abort at SIGSEGV
      # because there is no display server / GL backend on the runner.
      - uses: pyvista/setup-headless-display-action@v3

      - name: Install dependencies
        run: |
          uv sync --extra all

      - name: Build docs
        run: |
          cd docs
          uv run make html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/_build/html
          publish_branch: gh-pages
          force_orphan: true
